Testing System
==============

If a file xyz.valid is found, xyz is run and compared against the valid file.

If they are equal (watch for trailing newlines), the test succeeds. If the two files are different in any line, the test failes.
